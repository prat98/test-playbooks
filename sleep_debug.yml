- hosts: all
  gather_facts: false
  vars:
    num_messages: 50
  tasks:
    - name: Sleep and debug in sequence
      debug:
      	msg: "This is a debug message: {{ item }}"
      with_sequence: 'count={{ num_messages }}'
      loop_control:
      	pause: 5
